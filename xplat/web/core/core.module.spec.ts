import { LlstarscreamllCoreModule } from './core.module';

describe('LlstarscreamllCoreModule', () => {
  it('should work', () => {
    expect(new LlstarscreamllCoreModule(null)).toBeDefined();
  });
});
